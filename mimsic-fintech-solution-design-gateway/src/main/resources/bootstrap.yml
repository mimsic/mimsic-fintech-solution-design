---
spring:
  application:
    name: gateway
  cloud:
    config:
      uri: http://localhost:8888
      username: root
      password: s3cr3t
      fail-fast: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
  security:
    enabled: false